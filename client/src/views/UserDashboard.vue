<template>
  <div>
    <h1>Welcome EV User</h1>

    <p>You are successfully logged in.</p>

    <button @click="logout">Logout</button>
  </div>
</template>

<script setup>
import { useRouter } from "vue-router";

const router = useRouter();

const logout = () => {
  router.push("/");
};
</script>